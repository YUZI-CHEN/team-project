window.onload = function () {
  // 取得元素
  const circle = document.querySelector('.circle');
  const tick = document.querySelector('.tick');

  // 啟動動畫
  circle.style.strokeDashoffset = 0; // 圓圈繪製動畫
  tick.style.strokeDashoffset = 0; // 勾繪製動畫
};